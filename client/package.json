{
  "name": "gepa-viz-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @gepa-viz/app",
    "build": "npm run build -w gepa-viz && npm run build -w @gepa-viz/app",
    "build:lib": "npm run build -w gepa-viz",
    "build:app": "npm run build -w @gepa-viz/app",
    "lint": "npm run lint -w gepa-viz && npm run lint -w @gepa-viz/app",
    "preview": "npm run preview -w @gepa-viz/app"
  }
}
